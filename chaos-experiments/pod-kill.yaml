apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: frontend-kill
  namespace: chaos-testing
spec:
  action: pod-kill
  mode: one
  selector:
    namespaces:
      - staging
    labelSelectors:
      app: frontend
  duration: '5m'